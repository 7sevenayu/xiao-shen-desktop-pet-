# 🐟 让一个动漫少女住进你的桌面：DeepSeek 桌宠「小深」开发实录

> 一个基于 Electron 的 Windows 桌面宠物，从「一张图片」到「会呼吸、会打盹、会吃文件、能播音乐」的完整产品，中间踩过的每一个坑，都在这里。

---

## 一、缘起：为什么要做一只桌宠

一切始于桌面上的一张图——一位蓝发动漫少女deepseek的立绘。想让她「活」过来：会漂浮、会呼吸、会朝你的光标歪头，还能顺手干点正事。

于是有了「小深」，一个常住 Windows 桌面的 Electron 桌宠：

- **能干活的垃圾桶**：把文件拖到她身上，播放咀嚼动画，然后丢进系统回收站（不是永久删除，误删可还原）
- **DeepSeek 入口**：双击打开 DeepSeek 网页版，右键一键启动 dsh 命令行工具
- **会表演的演员**：静止站立、休息、挥手、跳跃、打盹、拖拽奔跑，全都有真帧动画
- **你的快捷面板**：音乐控制、自定义网站入口（一级常用 / 二级更多）、换肤、开机自启

最终形态是一行代码都嫌多、每个像素都有故事的 3 万行 JavaScript + Python 管线。

---

## 二、技术骨架：三个进程，各司其职

Electron 桌宠的本质是一个**透明、无边框、永远置顶、不抢键盘焦点**的窗口：

| 进程 | 职责 |
|---|---|
| `main.js` | 窗口/托盘/开机自启/皮肤持久化/拖动钳制/回收站/媒体键模拟 |
| `preload.js` | `contextBridge` 安全桥接（渲染进程无 Node 权限） |
| `renderer.js` | 动画引擎、抠像蒙版、鼠标穿透、交互、菜单、全部 UI |

几个关键的窗口参数：

```js
new BrowserWindow({
  transparent: true,      // 透明背景，露出桌面
  frame: false,           // 无边框
  alwaysOnTop: true,      // 悬浮在所有窗口之上
  focusable: false,       // 永不抢键盘焦点——回车永远属于你的应用
  skipTaskbar: true,      // 不进任务栏
});
```

**透明区域的点击穿透**是桌宠的灵魂：只有角色身体「看得见摸得着」，其余像素全部让鼠标直达桌面。实现上不是靠简单的 `setIgnoreMouseEvents`，而是**每一帧都在做像素级判定**：

1. 程序启动时对立绘做 `getImageData` 抠图，生成一张 0/1 蒙版（哪块是身体、哪块是透明）
2. 光标每移动一帧，把光标坐标换算进蒙版，命中身体 → 窗口可点击；命中透明区 → `setIgnoreMouseEvents(true, { forward: true })`
3. 图集动画的每一帧都有自己的蒙版（镜像帧还要水平翻转蒙版）

这套系统让「桌宠」真的像一只宠物趴在桌面上，而不是一个矩形窗口。

---

## 三、核心战役（一）：拖动 —— 八个坑，血泪铺成的 1:1 跟手

「把宠物抱起来换个位置」是第一个需求，也是最折磨人的一个。**1:1 跟手、方向正确、不闪烁、不丢窗口**，这四个要求背后是八个实打实的坑：

| # | 坑 | 现象 | 解法 |
|---|---|---|---|
| 1 | `.bat` 用了 LF 换行 | cmd 解析错乱 | 写 CRLF |
| 2 | 启动参数尾随反斜杠 | 转义引号 → 找不到应用 | 传 `.` |
| 3 | CSP 拦 `file://` 图片 | 图片加载失败 | 移除 CSP，改 IPC dataURL |
| 4 | `const petAPI` 与 preload 全局重名 | **整个脚本解析失败** | 直接使用全局 |
| 5 | 用 `clientX` 算位移 | **半速跟随**（光标 770px，宠物 385px） | 改用屏幕坐标 `screenX/screenY` |
| 6 | 用 `movementX` 算位移 | 位移在零附近**震荡** | 弃用，方向判定用 `screenX` 增量 |
| 7 | 指针捕获残留 | 「幽灵拖动」——点别处宠物跳过去 | 显式 `releasePointerCapture` + `elementFromPoint` 按下校验 |
| 8 | `window.moveBy` | **Windows 上会改变窗口大小**，越拖越大直到全屏卡死 | 禁用，改用 `win.setBounds({x, y, width, height})` 同时钉死位置和尺寸 |

其中最反直觉的是第 8 个：`window.moveBy` 在 Windows 上不是「移动窗口」，而是会**悄悄改变窗口尺寸**。日志里窗口从 282×332 一路膨胀到 1708×912。排查时靠的是 `pet-log.txt` 里每 15 帧一条的拖动日志——**日志驱动调试**是这个项目最重要的方法论。

最终的拖动链路：

```
pointerdown → elementFromPoint + 蒙版双重校验 → 记录 screenX/Y → setPointerCapture
pointermove → 绝对位移（相对按下点）→ requestAnimationFrame 节流
main 进程  → win.setBounds({ x, y, width: PET_W, height: PET_H })  // 尺寸钉死
pointerup  → 显式释放指针捕获
```

---

## 四、核心战役（二）：置顶与壁纸层 —— 和 Wallpaper Engine 抢地盘

桌宠要求「永远在最上」，但用户桌面装了 **Wallpaper Engine 动态壁纸**——它的渲染窗口直接挂进桌面层，接管了 Windows 桌面窗口结构（Progman/WorkerW）。这一仗打了五个版本：

1. **初版**：Electron 的 `setAlwaysOnTop(false)` 在 `focusable:false` 窗口上**根本不生效**（Electron #45024）→ 加原生 `SetWindowPos` 兜底
2. **沉到桌面层**：用 `HWND_BOTTOM` 把宠物放到壁纸之上、应用之下 → 实测可用
3. **挂靠 WorkerW**：试图挂到壁纸层子窗口 → **用户机器上三次执行失败**，回退 `HWND_BOTTOM`
4. **根因**：`GetWindow(GW_HWNDFIRST/LAST)` 在 Win11 上**恒返回 NULL**，z 序遍历从未生效，宠物被钉到壁纸之下「消失」→ 改用 `EnumWindows` 枚举 + 定位壁纸层窗口
5. **置顶守护进程**：每秒把宠物贴回「壁纸层窗口之后」（WE 重排会盖住宠物）；守护意外退出自动重启

还发现一个隐蔽 bug：`spawn(..., {detached:true})` 在 Windows 下会让 PowerShell **不执行脚本就退出**——守护进程从没真正跑起来过。

这一仗的结论：**桌面环境千奇百怪，永远不要假设窗口层级是「标准」的；原生 API 的返回值要打日志验证，而不是想当然**。
到最后的结果是删除了置顶功能。

---

## 五、核心战役（三）：动画资产 —— 提示词 → 视频 → 图集的生产管线

桌宠最初只有一张静态图。要让她动起来，两条路：

### 路线 A：图片版（已废弃）
用提示词让文生图模型逐帧生成帧带 → 拼成精灵图集。最终效果不达预期，整套管线废弃（素材留档）。

### 路线 B：视频版（最终方案）✅
**图生视频 + 程序化抽帧**，这才是桌宠动画的正解：

```
提示词（prompts-video/，8 套动作时间轴脚本）
  → 图生视频（可灵 / Runway / 即梦）
  → video2atlas.py 抽帧拼图集
  → skins/小深视频.png + 小深视频.layout.json
```

`video2atlas.py` 是这个管线的核心，解决了一连串视频处理的刁钻问题：

- **逐帧边缘采样抠像**：视频背景中途变色（品红 → 绿色）？中位数法会误删静止角色 → 改为每帧从边缘采样背景色再抠
- **质量闸门**：抽帧不清晰（运动模糊/过渡帧）→ 质量评分低于阈值自动偏移重抽相邻帧
- **水印清除**：跨帧静止像素蒙版——水印不动而角色在动，静止像素就是水印
- **绿色残留**：绿幕杂色被双腿包围（不触边）→ 洪水填充清除 + 封闭绿色连通块删除
- **质心对齐**：每帧角色位置漂移 → 按质心对齐再入格
- **布局文件**：`.layout.json` 支持每套动作独立帧数/时长/镜像（拖拽左右共用一行，跳跃/睡觉各有变体）

最终产出 **1536×1872 图集（8 列 × 9 行，每帧 192×208）**，9 行满配 8 套动作。渲染端是一个 2 倍分辨率、逐帧融合的图集引擎：

- 静止站立默认**定格在第 0 帧**，每 7~13 秒播一段 2.4 秒站立动作
- 动作**不可打断 + 排队**：播放中的动作必须播完，新动作排队等待
- 帧间融合（透明度混合 + 缓出曲线），慢帧率动作不再硬切跳变
- 45 秒无交互自动打盹，醒来先伸懒腰

---

## 六、核心战役（四）：菜单架构三迁 —— 为了不暂停浏览器里的视频

右键菜单经历了三次架构重写，每次都被同一个问题逼疯：**打开菜单不能暂停浏览器里播放的视频**。

| 方案 | 问题 |
|---|---|
| ① 窗口内菜单 | 判定范围小、点外面关不掉 |
| ② 窗口临时加宽（宠物左侧放菜单） | 宠物闪现、菜单落屏外 |
| ③ 全屏捕获层（独立全屏窗口渲染菜单） | **遮挡整个屏幕 → 浏览器视频被暂停**（后台节流/覆盖窗口） |

最终方案是「窗口内菜单 + 临时可聚焦」：

1. 窗口宽度 280 → **560**，左侧 340px 是菜单/透明区（点击穿透由蒙版保证）
2. 菜单打开时 `win.setFocusable(true) + win.focus()` —— **临时获得焦点**
3. 点桌面/任务栏/浏览器任意处 → 窗口失焦 → `blur` 事件 → 关闭菜单 + 恢复 `focusable:false`

没有全屏覆盖层，浏览器永远不会被遮挡，视频自然不停。菜单定位还考虑了用户是右撇子：**优先弹在人物左侧**，右侧留给屏幕边缘操作。

这一仗的教训：**「遮挡」和「焦点」是桌宠世界的两个隐形敌人，任何一个全屏层都会带来连锁副作用；能用窗口内解决的就不要开新窗口**。

---

## 七、功能盘点：最终形态

- 🖱️ **拖动**：1:1 跟手、贴边钳制、拖拽动画（被手托住）
- 🗑️ **垃圾桶**：拖文件 → 咀嚼动画 → 系统回收站
- 🎵 **音乐控制**：模拟媒体键（`keybd_event` VK_MEDIA_*），系统路由到当前媒体会话——QQ 音乐/网易云/Spotify 通用
- 🌐 **网站快捷入口**：一级常用（网格图标）/ 二级更多（可展开、可返回），「管理网站」面板支持增删/排序/调级，持久化到 `pet-config.json`；favicon 三级兜底（本站 → Google 图标服务 → emoji）
- 🎨 **换肤**：经典蓝 + 视频图集皮肤，支持导入 1536×1872 图集自动识别为动画
- 🚀 **开机自启**：注册表 `Run` 项，可开关
- 🤖 **启动 dsh**：端口 3080 智能检测（已运行则打开网页）
- 📋 **剪贴板路径**：打开文件夹 / 定位文件 / 打开网址
- 😴 **打盹**：45 秒无交互入睡，循环睡觉动画
- 📌 **常驻置顶**：悬浮所有窗口之上（恒开，从根上绕开置顶 bug）

## 八、工程习惯：这个项目怎么活下来的

1. **日志驱动调试**：`pet-log.txt` 记录每次拖动的屏幕坐标、窗口位置、尺寸——没有日志，半速拖动、moveBy 改尺寸这类问题根本无法定位
2. **每轮改动固定验证流程**：`node --check` 语法检查 → 杀旧进程 → 清空日志 → 用户重启实测 → 读日志确认
3. **自检脚本**：`npm run selftest` 13 项自动化回归（渲染初始化/图集/换肤往返/拖动/置顶/网站校验/截图）
4. **交接文档**：25 稿修改记录，每个坑都写「现象 → 根因 → 解法」，防止回退复发

---

## 九、写在最后

桌宠开发最大的难点从来不是「让它动」，而是**让它住进真实桌面环境的夹缝里**：透明的像素、永不抢的焦点、壁纸层的层级、浏览器的视频、用户的右撇子习惯……每一个都是「看起来简单，做起来全是边界情况」。

如果你也要做类似的东西，记住三条：

- **桌面窗口 API 的返回值永远要打日志验证**，特别是 Windows 上（`moveBy`、`GetWindow`、`detached` 各有各的坑）
- **不要开全屏覆盖层**，它会暂停别人正在看的一切
- **动画资产用「图生视频 + 抽帧」而不是逐帧生成**，质量和成本都天差地别

现在，「小深」还趴在屏幕右下角，每隔一会儿伸个懒腰。双击她，DeepSeek 就打开了。

---

*技术栈：Electron 34 · JavaScript · Node 24 · Python 3.13（OpenCV/PIL） · 图生视频（runinghub）*
*项目：`E:\node\desktop-pet` · 生成管线：`E:\node\pet-hatch`*
