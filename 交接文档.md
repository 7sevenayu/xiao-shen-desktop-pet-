# 🐟 DeepSeek 桌宠「小深」交接文档

> 最后更新：2026-08-16（二十四稿）
> 适用环境：Windows + Node v24 + Python 3.13（py 启动器）

---

## 0. 修改记录

| 日期 | 变更 |
|---|---|
| 2026-08-16 | 定稿当前形态：单皮肤、拖动最终方案、图集引擎保留待用 |
| 2026-08-16（二稿） | 修正 FAQ 的 size/viewport 期望值与实测不符；补关键常量代码锚点；补「视频抽帧→图集」工具规格草案；修正 about 面板「3 套皮肤」文案 |
| 2026-08-16（三稿） | 修复置顶开关 bug：Electron 34 下 `focusable:false` 窗口的 `setAlwaysOnTop(false)` 不生效（#45024），`main.js` 增加原生 `SetWindowPos` 兜底（`setTopmostNative`）；托盘「显示桌宠」改 `show()+moveTop()` |
| 2026-08-16（四稿） | 置顶关闭后的落点改为**桌面层**（`HWND_BOTTOM`：壁纸之上、所有应用窗口之下），不再是普通窗口带顶部；托盘「显示桌宠」与二次启动统一走 `showPet()`，按置顶状态决定是否抬升 |
| 2026-08-16（五稿） | 置顶关的落点升级为**挂靠桌面 `WorkerW`**（壁纸层子窗口：壁纸之上、图标/窗口之下，Win+D 显示桌面也不会清除它）；`showPet()` 增加 `restore()`（修复 Win+D 最小化后托盘唤不出）；托盘菜单新增「📌 置顶」开关；窗口意外销毁（explorer 重启连带销毁 WorkerW 子窗口）自动重建 |
| 2026-08-16（六稿） | **回退 WorkerW 方案**：该 PowerShell 脚本在用户机器上三次执行失败（14:44 日志 native=false，置顶关失效），落点回归实测可用的 `HWND_BOTTOM`（13:32/13:36/13:45 五次 native=true 且可见可拖）；原生调用失败日志增加 stderr 捕获；`restore()`/托盘置顶/自动重建等修复保留 |
| 2026-08-16（七稿） | 查明 WorkerW 失败的根因：**用户桌面使用动态壁纸 Wallpaper Engine**（WE 渲染窗口挂进桌面层，接管 Progman/WorkerW 结构）。置顶关落点 `HWND_BOTTOM` 不依赖桌面窗口结构，WE 场景下实测可见；后续若遇壁纸层覆盖问题，按 WE 窗口结构适配（备选：置顶关期间定时重贴底） |
| 2026-08-16（八稿） | 修复「关掉当前窗口后宠物被 WE 壁纸盖住」：新增**置顶守护进程**（`startTopmostWatcher`，置顶关期间每秒把宠物贴回「壁纸层窗口之后」——识别 Progman/WorkerW 类或 `wallpaper*` 进程为壁纸层）；一次性贴底脚本增加 z 序诊断输出；退出/切悬浮时自动终止守护（`will-quit`） |
| 2026-08-16（九稿） | 置顶守护频率 2 秒 → **1 秒**（`Start-Sleep -Seconds 1`），WE 重排后宠物更快贴回壁纸层 |
| 2026-08-16（十稿） | 置顶守护增加**意外退出自动重启**（3 秒后重试、连续 3 次失败停止并记录日志）；守护占用实测：CPU ≈ 0%、工作集约 60~80 MB（仅置顶关时存在一个 powershell 进程） |
| 2026-08-16（十一稿） | **新动作上线**：① 图片版 5 套新动作（jumping/failed/waiting/running/review）拼装完成，`skins\xiao-shen-manual.png` 全 9 行；② `video2atlas.py` 重写为 9 行满配（8 套动作：idle 16 帧 + 其余 7 套各 8 帧，跳跃双视频自动择优），产出 `skins\小深视频.png` + `.layout.json`；③ renderer 随机小动作池改为 layout 感知并纳入 waiting/review；④ GLM-4V 视觉质检全部通过 |
| 2026-08-16（十二稿） | **删除 `xiao-shen-manual` 皮肤**（图片版实际效果不达预期，用户弃用）；帧带素材仍备份在 `E:\node\pet-hatch\manual\`，如需再生可重跑 `拼装图集.bat` |
| 2026-08-16（十三稿） | ① 图片版管线废弃：`manual\` 素材目录整体删除（提示词 `prompts\`、`extract_fixed.py` 保留，`extract_fixed` 仍被 `video2atlas.py` 引用）；② **新增打盹行为**（`renderer.js`）：45 秒无交互自动入睡并循环睡觉动画，点击/拖动/投喂/右键唤醒（醒来先伸懒腰），随机小动作抽到「打盹」也会入睡，睡眠时显示 💤 气泡且 tips 不打扰，换肤重置打盹状态 |
| 2026-08-16（十四稿） | **默认形态改为「静止站立」（review 状态）**，其他动作按触发体系出现：时间（每 13~29 秒随机抽：休息/挥手/跳跃/打盹）+ 鼠标（点击→跳跃、拖拽→左右跑、**悬停→打招呼** 20 秒冷却、投喂→吃）+ 45 秒无交互打盹 |
| 2026-08-16（十五稿） | **站立保持（防呆站）**：默认站立大部分时间定格在第 0 帧（`STAND_HOLD_FRAME`），每隔 7~13 秒才播一段约 2.4 秒的站立动作（`STAND_MOVE_MS`），播完回到定格；离开站立状态（拖拽/吃/睡/随机动作）自动重置节奏。**动作整体优化**：随机动作按类型定制时长（休息 1.8s/挥手 1.5s/跳跃 1.2s），醒来伸懒腰缩短到 0.9s |
| 2026-08-16（十六稿） | **拖拽动作替换左右奔跑**：新增视频 `抓住.mp4`（角色被手托住、正对观众、左右对称），`video2atlas.py` 的 running-right/running-left 两行都改用该视频（无需镜像，GLM 质检通过）；睡觉增加 `睡觉2.mp4` 候选自动择优 |
| 2026-08-16（十七稿） | **动作时长与衔接优化**：① `video2atlas.py` 每帧时长改为按「源视频时长/帧数」自动计算（`speed` 倍率微调）——拖拽 130→651ms/帧、站立 280→651ms 等，修复抽帧动画被 5 倍速播放；② 渲染器新增**交叉淡化过渡**（`snapshotForTransition`，TRANS_MS=170）：状态切换与站立回定格时新旧画面按透明度混合，衔接不再生硬；③ `STAND_MOVE_MS` 调整为 3300ms 匹配自然帧节奏 |
| 2026-08-16（十八稿） | **新增音乐控制**：播放/暂停、上一首、下一首（右键菜单与托盘菜单；`main.js` 用 `keybd_event` 模拟媒体键 VK_MEDIA_*，系统路由到当前媒体会话，Spotify/网易云/QQ音乐等通用） |
| 2026-08-16（十九稿） | **动作不可打断 + 变体动画**：① 渲染器新增动作排队（`playAction`/`pendingAction`/`drainPendingAction`）——播放中的动作必须播完，新动作（点击/悬停/跳舞）排队等待，拖拽/投喂/入睡/换肤会丢弃排队；② **跳跃/睡觉各两套动画随机切换**（`variantOf`：跳跃/跳跃2、睡觉/睡觉2，每次执行随机挑一套，睡觉时用 `sleepState` 记住本次变体）；③ 图集改为 9 行满配：拖拽左右共用一行（`running-left` 用 `mirror` 映射，不占行）、idle 压缩到 8 帧，为两个变体行腾出行 |
| 2026-08-16（二十稿） | **帧间融合**：交叉淡化从「仅状态切换」扩展到**同一状态内每一帧**（`FRAME_BLEND_MS=130`），慢帧率动作（拖拽 651ms/帧、睡觉等）不再硬切跳变；状态切换过渡加长到 `TRANS_MS=200`；`snapshotForTransition(t, ms)` 支持自定义时长，淡化按实际时长计算 |
| 2026-08-16（二十一稿） | **修复动作时的「闪一下」**：根因是旧帧以 100% 不透明度叠加，姿态差异大的帧出现双重曝光鬼影。改为**叠加透明度上限**（帧间 30%、状态切换 50%）+ **二次缓出曲线** + 时长微调（`TRANS_MS=220`、`FRAME_BLEND_MS=120`），既柔和又不闪 |
| 2026-08-16（二十二稿） | **打包便携产品**：`E:\node\小深桌宠-win64.zip`（113 MB，免安装，自带 Electron 运行时）。结构：`小深桌宠/{启动桌宠.bat, README.txt, app/, electron/}`；启动脚本为 CRLF 纯 ASCII；构建目录 `E:\node\dist-build\小深桌宠` 保留可直跑。注意：与开发版共用 app 名 → userData/单实例锁相同，勿同时运行；开机自启指向便携版 electron.exe 路径 |
| 2026-08-16（二十三稿） | **工作区整理**：废稿/死链/缓存 15 项移入 `E:\node\待处置废稿\`（含 `说明.txt` 清单，由用户处置）：图片版提示词 `prompts\`、`xiao-shen\`、`生成精灵.bat`（.env 缺失已失效）、`拼装图集.bat`（manual 已删失效）、`pet-wheels\`、`pet-pip-cache\`、`pet-tmp\`、两处 `__pycache__`、`h3-prompts.md`、`video-prompts.md`、`vision-test.png`、`make-vision-test.ps1`、`build-product.ps1`、根目录 `package-lock.json` |
| 2026-08-16（二十四稿） | **修复脚下阴影不跟随人物**：窗口加宽到 560 后 `#pet-pos` 中心在 455px，而 `#shadow` 停留在老布局 left:343px（偏左 56px）。`renderer.js` 每帧把阴影 `left` 对齐 `petPos` 水平中心（`translateX(-50%)` 使 left 即中心），CSS 基线同步改为 399px |

---

## 1. 项目概况

Windows 桌面宠物（Electron v34），角色「小深」出自桌面原图 `1abb56c0...jpg`（蓝发动漫少女）。

**当前状态**：
- ✅ 全部核心功能可用，**拖动问题已彻底修复**（详见第 6 节，勿回退）
- ✅ 两套皮肤：**经典蓝**（静态）、**小深视频**（视频抽帧版 8 动作图集 + 自定义布局）
- ✅ 视频版 8 状态可用（failed 行留空，引擎自动回退）；图片版皮肤与素材已删除
- ✅ **打盹行为**：45 秒无交互自动入睡（循环睡觉动画），点击/拖动/投喂/右键唤醒
- ✅ **动作触发体系**：默认形态=静止站立；时间随机（休息/挥手/跳跃/打盹）+ 鼠标动作（点击跳、拖拽跑、悬停打招呼、投喂吃）

---

## 2. 目录与文件

### 桌宠本体 `E:\node\desktop-pet\`

| 文件 | 作用 |
|---|---|
| `main.js` | 主进程：窗口/托盘/自启/皮肤/剪贴板/拖动/回收站 |
| `preload.js` | contextBridge 安全桥接（含 webUtils、getBounds） |
| `renderer.js` | 渲染进程：动画/交互/抠图/换肤/图集引擎/鼠标穿透 |
| `index.html` / `style.css` | 界面与样式 |
| `assets\pet.jpg` | 默认立绘（自动去白底） |
| `skins\` | 皮肤目录：`小深视频.png` + `小深视频.layout.json`（视频版图集） |
| `pet-config.json` | 配置（skin / autoStart） |
| `pet-log.txt` | **诊断日志（重要，问题排查全靠它；未做自动轮转，长期运行会持续增长，排障后建议手动清空）** |
| `启动桌宠.bat` / `start-pet.bat` | 一键启动（CRLF 换行！） |
| `README.md` | 功能说明 |

### 生成管线 `E:\node\pet-hatch\`

| 路径 | 内容 |
|---|---|
| `prompts\` | ~~图片版提示词~~（二十三稿已移至 `E:\node\待处置废稿\prompts\`） |
| `prompts-video\` | **视频版提示词**（8 套动作的时间轴脚本，最新） |
| `manual\` | ~~手动帧带目录~~（十三稿已删除：图片版管线废弃，素材随删除） |
| `xiao-shen\` | ~~API 全自动路径配置~~（二十三稿已移至待处置废稿） |
| `extract_fixed.py` | **补丁版拼装脚本**（修复过：白裙误删/肢体消失/边缘羽化/去品红杂色/帧位微调/切边检测） |
| `拼装图集.bat` | ~~手动帧带 → 桌宠图集~~（已失效：manual 已删；二十三稿已移至待处置废稿） |
| `生成精灵.bat` | ~~API 全自动路径~~（`E:\node\.env` 不存在已失效；二十三稿已移至待处置废稿） |
| `video2atlas.py` | **视频抽帧→图集**（cv2 逐帧抠像 + 质量闸门 + 9 行满配布局，视频源默认 `C:\Users\22248\Desktop\新建文件夹`） |
| `manual-video\` | 视频版产物（图集/GIF 预览） |
| `E:\node\pet-deps\` | Python 依赖（PIL/numpy/cv2，PYTHONPATH 使用） |
| `E:\node\pet-wheels\` | ~~下载的 wheel 包~~（依赖已装入 pet-deps；二十三稿已移至待处置废稿） |

---

## 3. 功能清单（均已验证）

| 功能 | 入口 |
|---|---|
| 漂浮/摇摆/呼吸/果冻点击抖动/随机小动作 | 常驻 |
| 自动打盹（45 秒无交互入睡循环睡觉动画，**睡觉/睡觉2 随机一套**，点击/拖动/投喂/右键唤醒，醒来伸懒腰） | 常驻（图集皮肤） |
| 动作不可打断（播放中的动作播完才执行下一个，排队机制） | 常驻（图集皮肤） |
| 默认形态=静止站立（**定格第 0 帧**，每 7~13 秒动 2.4 秒）；时间随机动作（休息/挥手/跳跃）；悬停打招呼（20 秒冷却） | 常驻（图集皮肤） |
| 拖动（1:1 跟手，边界钳制防失踪；拖拽动画「抓住」） | 左键拖 |
| 双击开 DeepSeek 网页版 | 双击 |
| 启动 dsh（端口 3080 智能检测：已运行则打开网页） | 右键 |
| 打开剪贴板路径（文件夹/文件定位/网址） | 右键 |
| 音乐控制（播放/暂停、上一首、下一首，媒体键模拟，通用播放器） | 右键 / 托盘 |
| 垃圾桶（拖文件→咀嚼动画→回收站） | 拖文件到身上 |
| 换肤（经典蓝 / 小深视频 视频版；1536×1872 图集自动识别为真帧动画，配套 `.layout.json` 自动加载） | 右键 → 换肤 |
| 开机自启（注册表 Run\DeepSeekPet，可开关） | 右键/托盘 |
| 置顶开关（开=悬浮最上；关=沉到桌面层）、托盘常驻 | 右键/托盘 |
| 透明区域点击穿透 + 永不抢键盘焦点 | 自动 |
| 图集动画引擎（9 状态规格、2 倍渲染、平滑缩放） | 皮肤为图集时 |

---

## 4. 运行与诊断

```
启动：双击 E:\node\desktop-pet\启动桌宠.bat（只双击一次；单实例锁会拦截重复启动）
退出：右键宠物 → 退出；或托盘图标 → 退出
日志：E:\node\desktop-pet\pet-log.txt（每次拖动都会记录关键数据；无自动轮转，定期手动清空）
开机自启：注册表 HKCU\...\Run\DeepSeekPet = "electron.exe" "E:\node\desktop-pet"
```

---

## 5. 当前形态约定（2026-08-16 定）

1. **内置皮肤只剩「经典蓝」**（`BUILTIN_SKINS` 仅 classic，`crop: 1` 不裁剪）；
2. **图集皮肤仅留视频版**：`skins\小深视频.png`（配套 `小深视频.layout.json` 自定义布局：idle 16 帧占 2 行，其余 7 套各 8 帧 1 行）；图片版 `xiao-shen-manual` 已删除（用户弃用，素材留档 `E:\node\pet-hatch\manual\`）；
3. 帧带素材备份在 `E:\node\pet-hatch\manual\`，视频源在 `C:\Users\22248\Desktop\新建文件夹`；
4. 图集引擎代码保留并启用（`renderer.js` 的 `ATLAS_STATES`/`loadAtlas`/`getAtlasFrameMask`），随机小动作池含 waving/jumping/waiting/review。

---

## 6. ⚠️ 拖动方案（最终版，勿回退）

**这是踩了 8 个坑才定型的方案，任何回退都会复发问题。**

最终实现（renderer.js + main.js）：

1. `pointerdown`：校验按下点在角色身体上（`elementFromPoint` + 抠像蒙版双重校验）→ 记录 `e.screenX/screenY` → `setPointerCapture`
2. `pointermove`：发 **screenX/screenY 的绝对位移**（相对按下点）→ rAF 节流
3. 主进程：`win.setBounds({ x, y, width: PET_W, height: PET_H })` —— **同时钉死位置和尺寸**
4. `pointerup/cancel`：显式 `releasePointerCapture`

### 踩坑清单（血泪史）

| # | 坑 | 表现 | 解决 |
|---|---|---|---|
| 1 | .bat 用 LF 换行 | cmd 解析错乱（`tron.exe`） | 写 CRLF |
| 2 | 启动参数尾随反斜杠 | 转义引号→找不到应用 | 传 `.` 或去尾斜杠 |
| 3 | CSP 拦 file:// 图片 | 图片加载失败 | 移除 CSP，改 IPC dataURL |
| 4 | `const petAPI` 与 preload 全局重名 | 整个脚本解析失败 | 直接使用全局 |
| 5 | `clientX` 相对窗口算位移 | **半速跟随** | 用屏幕坐标 |
| 6 | `movementX` 在此机器上**也不可靠** | 位移在零附近震荡 | 弃用 |
| 7 | 指针捕获残留 | 「幽灵拖动」（点别处宠物跳过去） | 释放捕获+按下校验 |
| 8 | **`window.moveBy` 在 Windows 上会改变窗口大小** | 窗口越拖越大直到全屏卡死 | 禁用，改用 setBounds |

### 本机关键参数

- 屏幕：**1707×912 DIP（1920×1080 @ 112.5% 缩放）**，单显示器
- 桌面环境：**动态壁纸 Wallpaper Engine**（渲染窗口挂进桌面层，接管 Progman/WorkerW 结构——因此置顶关落点用 `HWND_BOTTOM`，**勿改回 WorkerW 挂靠方案**）
- 窗口：280×330（常量 `main.js` 的 `PET_W/PET_H`）；拖动边界 y ∈ [-60, 912-330+44]，x ∈ [0, 1707-280]（偏移 60/44 写死在 `main.js` 的 `drag-move` 钳制里）
- 图集：1536×1872，8 列×9 行，每帧 192×208（常量 `ATLAS_W/H`、`CELL_W/H`）；行规格见 `renderer.js` 的 `ATLAS_STATES`
- 已知现象（勿当 bug）：**每次启动后的第一次拖动前**，`getSize()` 会短暂为 282×332（Windows 给无边框窗口加的 1px 不可见边框），首次 `setBounds` 后即钉回 280×330 并保持

---

## 7. 动作生成管线（两条路）

### A. 图片版（拼图集 → 桌宠）——已废弃
图片版提示词已随二十三稿移入 `E:\node\待处置废稿\`；`extract_fixed.py` 保留（`video2atlas.py` 仍在引用其抠图/对齐函数）。如需重建图片管线：从待处置废稿取回提示词、重新生成帧带。

### B. 视频版（已可用）
- 提示词在 `prompts-video\`（8 套时间轴脚本，含循环衔接要求）
- 图生视频（可灵/Runway/即梦等）上传基准图 + 贴提示词，成品放 `C:\Users\22248\Desktop\新建文件夹`
- **工具已落地**：`video2atlas.py` 把视频抽帧拼图集（逐帧边缘采样抠像 + 质量闸门 + 多候选择优），运行 `py -3.13 E:\node\pet-hatch\video2atlas.py` 即自动更新 `skins\小深视频.png` + `.layout.json`

**「视频抽帧→图集」工具（video2atlas.py，已实现上述草案）**：抽帧用 cv2 按视频帧位置均匀采样（不合格帧自动偏移重抽）；抠像用逐帧边缘采样背景色（兼容中途变色的背景）+ 连通域去背 + 羽化；复用 `extract_fixed.py` 的碎点清理与质心对齐；按 LAYOUT 拼 1536×1872 并自动写 `.layout.json`。跑之前改 `LAYOUT` 表即可增删动作。

---

## 8. 常见问题速查

| 问题 | 处理 |
|---|---|
| 宠物不动/乱动 | 看 `pet-log.txt` 的 drag-start（viewport 与 size 应恒为 280x330；例外：启动后第一次拖动前会短暂 282x332，属 Windows 无边框窗口的 1px 边框，首次拖动即被修正） |
| 置顶关后宠物被窗口盖住/找不到 | 这是桌面部件语义（壁纸之上、窗口之下）；托盘菜单勾选「📌 置顶」即可唤回最上；按 Win+D 后被清掉则点托盘「显示桌宠」（会自动 restore） |
| 置顶关后宠物被 WE 壁纸盖住（偶发） | 八稿已内置守护自动处理（每秒贴回壁纸层之上）；若仍被盖住，查日志确认「置顶守护启动」存在且 `pet-log.txt` 无「原生置顶调用失败」 |
| 双击 bat 没反应 | 已有实例在跑（单实例锁）；托盘退出后再启动 |
| 图片不显示 | 查日志 `buildSprite/皮肤` 段；确认 `assets\pet.jpg` 存在 |
| 拼装脚本报缺模块 | `$env:PYTHONPATH="E:\node\pet-deps"; py -3.13 ...` 运行 |
| 视频版皮肤更新 | 新视频放进 `C:\Users\22248\Desktop\新建文件夹` → 改 `video2atlas.py` 的 `LAYOUT` 表 → `py -3.13 E:\node\pet-hatch\video2atlas.py` → 重启桌宠换肤 |
| `pet-config.json` 损坏/配置丢失 | 退出桌宠后删除该文件，重启会自动重建默认配置，再重新导入皮肤 |
| 换显示器/分辨率后拖动错乱 | 桌面边界只在启动时探测，重启桌宠即可重新计算 |
| 导入同名皮肤后列表出现重复 | 当前实现会覆盖 `skins\` 下同名文件且列表不去重，导入前先改名 |
| 想恢复被删皮肤/废稿 | 二十三稿整理：图片版提示词等在 `E:\node\待处置废稿\`；更老的素材在 `E:\pet\hatch-pet-claude-main\examples\`；视频版素材在 `C:\Users\22248\Desktop\新建文件夹` |
| 宠物一直睡觉/不醒 | 十三稿新行为：45 秒无交互自动入睡；点击或拖动宠物即可唤醒（睡梦中 tips 不会打扰它） |

---

## 9. 下一步建议

1. **新动作持续补充**：新视频 → `video2atlas.py` 的 `LAYOUT` 表（注意总行数 ≤ 9：16 帧动作占 2 行、8 帧占 1 行）。若改行规格，同步改 `renderer.js` 的 `ATLAS_STATES`；行为逻辑（打盹等）在 `renderer.js` 的 `startSleeping`/`wakeUp`
2. **可选增强**：打包 exe（electron-builder）、语音气泡、跟随鼠标模式
3. 每轮改动的验证习惯：**语法检查（node --check）→ 关闭旧进程 → 清空 pet-log.txt → 用户重启实测 → 读日志确认**
